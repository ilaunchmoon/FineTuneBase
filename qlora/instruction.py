"""
    Qlora思想

        即在Lora微调的基础上, 不仅仅是将基座模型的冻结住, 而且还将基座模型的权重在微调训练的过程中进行4Bit或8Bit量化操作, 等微调结束后又将基座模型恢复为非量化的原有状态

        双重量化: 基座模型经过NF4分位量化后, 每个块需要存放0.5bit的量化常数, 双重量化是将这些量化常数再次进行量化

        显存分页优化器: 当显存不足时, 将优化器的参数转移到CPU内存上, 在需要时再将其取回, 防止在显存峰值时造成显存OOD
    
        
    Qlora的操作步骤

        

"""